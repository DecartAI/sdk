# The API key should be private and never exposed in the client app.
# We're using it here for demo purposes, but for production apps
# you should keep your keys secret, e.g. on your backend.
VITE_DECART_API_KEY=your-api-key-here
